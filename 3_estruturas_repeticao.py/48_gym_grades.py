count = 0
name = input('Digite o nome')

grade = float(input('Digite a nota'))
up = grade
down = grade
sum = grade - grade

while count < 6:
    grade = float(input('Digite a nota'))
    if grade > up:

    elif grande < down:

    else